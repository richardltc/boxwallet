# Zig build cache and output
.zig-cache/
zig-out/

# Vendored dependency / global cache dir (ZIG_GLOBAL_CACHE_DIR=zig-pkg).
# Regenerated by `zig fetch` / the build; not committed.
zig-pkg/

# Local install root produced by running the app (downloaded coin daemons).
boxwallet-coins/
